<article class="meta-card">
  <p class="eyebrow">{{ projectView.labels.role }}</p>
  <p>{{ projectView.role }}</p>
</article>
<article class="meta-card">
  <p class="eyebrow">{{ projectView.labels.timeline }}</p>
  <p>{{ projectView.timeline }}</p>
</article>
<article class="meta-card">
  <p class="eyebrow">{{ projectView.labels.tools }}</p>
  <p>{{ projectView.tools | join(", ") }}</p>
</article>
<article class="meta-card">
  <p class="eyebrow">{{ projectView.labels.focus }}</p>
  <p>{{ projectView.focus }}</p>
</article>
