---
layout: layouts/base.njk
eleventyComputed:
  title: "{{ site.pages.work.seo.title }}"
  description: "{{ site.pages.work.seo.description }}"
---
<section class="page-intro">
  <p class="eyebrow">{{ site.pages.work.eyebrow }}</p>
  <h1>{{ site.pages.work.headline }}</h1>
  <p class="page-lead">{{ site.pages.work.intro }}</p>
</section>

<section class="section-block">
  <div class="project-grid project-grid-full">
    {% for project in collections.projects %}
      {% include "partials/project-card.njk" %}
    {% endfor %}
  </div>
</section>
